{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma db push --skip-generate && tsx scripts/ensure-admin.ts && next build",
  "installCommand": "npm install --legacy-peer-deps",
  "regions": ["bom1"]
}
